## TODO

- [ ] Update `view/product-order.ejs` to place product image on the left and customer order form/details on the right (2-column layout).
- [ ] Update `public/style.css` to style the new 2-column layout and add responsive stacking on small screens.
- [ ] (After edits) Run/reload app and verify layout at `/products/:id/order`.

